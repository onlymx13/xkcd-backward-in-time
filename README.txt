Get assets (required before first run): `npm run build_assets`
Clean assets: `npm run clean_assets`
Run: `npm run vite`

TODO:
* CSS
* Historical events within 10% of T from (Now - T). Looking at using Wikidata queries to implement this
* TODOs in the code/repo
